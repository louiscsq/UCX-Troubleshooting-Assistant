targets:
  dev-lakebridge:
    variables:
      config_file: "configs/lakebridge.config.yaml"
      repo: "databrickslabs/lakebridge"
      project_prefix: "lakebridge"
    workspace:
      host: ${var.workspace_host}
  
  prod-lakebridge:
    variables:
      config_file: "configs/lakebridge.config.yaml"
      repo: "databrickslabs/lakebridge"
      project_prefix: "lakebridge"
    workspace:
      host: ${var.workspace_host}

