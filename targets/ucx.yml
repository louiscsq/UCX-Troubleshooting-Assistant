targets:
  dev-ucx:
    default: true
    variables:
      config_file: "configs/ucx.config.yaml"
      repo: "databrickslabs/ucx"
      project_prefix: "ucx"
    workspace:
      host: ${var.workspace_host}
  
  prod-ucx:
    variables:
      config_file: "configs/ucx.config.yaml"
      repo: "databrickslabs/ucx"
      project_prefix: "ucx"
    workspace:
      host: ${var.workspace_host}

